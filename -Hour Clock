# Prompt the user to enter the present time
present_time = int(input("Please enter the present time (in 24-hour format): "))

# Request the number of hours to wait (renamed to delay_hours)
delay_hours = int(input("Please enter how many hours to delay the alarm: "))

# Calculate future time
future_time = (present_time + delay_hours) % 24

# Display the result
print(f"The alarm will go off at {future_time}:00 on the 24-hour clock.")
